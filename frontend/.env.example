# =============================================================
# 前端环境变量示例（本文件提交到仓库，作为模板）
# 复制为 .env.development 并按需填写；真实取值勿提交。
#
# VITE_API_BASE
#   仅「开发模式」生效：指定前端请求的控制面后端地址。
#   留空（默认）时，前端走同源 /api，由 vite.config.js 的代理转发到
#   http://localhost:9000，从而避免跨源 (CORS)。
#   仅当你希望绕过本地代理、直连远程控制面时才填写完整地址，例如：
#     VITE_API_BASE=https://control.example.com/api
#
# 生产模式不受本变量影响，运行时配置始终读取 public/cloudshuttle-config.js。
# =============================================================
VITE_API_BASE=